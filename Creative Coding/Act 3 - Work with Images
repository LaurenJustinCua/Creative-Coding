let rubble
let space

function preload(){
  space = createImg('space.jpg')
  rubble = createImg('rubble.jpg')
}

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  rubble.position(125,100)
  rubble.size(150,150)
  
  space.position(0,0)
  space.size(400,400)
}
