function setup() {
  createCanvas(800, 800);
  noLoop();
}

function draw() {
  background(100);
  let gridSize = 50; 
  
  for (let y = 0; y < height; y += gridSize) {
    for (let x = 0; x < width; x += gridSize) {
      circle(x, y, gridSize);
    }
  }
}
